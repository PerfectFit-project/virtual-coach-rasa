version: "3.0"  # orig 3.0
services:
  rasa_server:
    image: rasa/rasa:2.8.1-full
    ports:
      - 5005:5005
    volumes:
      - ./:/app
    command:
      - run
  rasa_actions:
    build: actions/
    expose: ["5055"]
